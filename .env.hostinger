# HOSTINGER PRODUCTION ENVIRONMENT VARIABLES
# Replace the placeholder values below with your actual credentials

# REQUIRED: Get from Hostinger -> Databases -> MySQL
DATABASE_URL=mysql://YOUR_USERNAME:YOUR_PASSWORD@localhost/YOUR_DATABASE_NAME

# REQUIRED: Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=301d6a034fec732305b18772883b954db244d90075aa688da2b32bff8719f836

# REQUIRED: Production mode
NODE_ENV=production

# REQUIRED: Port number
PORT=3000

# OPTIONAL: Email configuration (needed for sending invoices/estimates)
# Get API key from https://resend.com
RESEND_API_KEY=re_YOUR_RESEND_API_KEY_HERE
FROM_EMAIL=onboarding@resend.dev
PUBLIC_URL=https://your-app.hostinger.site

# OPTIONAL: Only if you use price scraping features
# VALUESERP_API_KEY=your_valueserp_key_here
# SCRAPINGFISH_API_KEY=your_scrapingfish_key_here
